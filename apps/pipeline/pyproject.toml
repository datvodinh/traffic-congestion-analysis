[tool.poetry]
name = "pipeline"
version = "0.1.0"
description = ""
authors = ["datvodinh <vodinhdat03@gmail.com>"]
readme = "README.md"
packages = [
  {include = "src"}
]

[tool.poetry.dependencies]
python = ">=3.11,<3.12"
dagster = "^1.8.11"
dagster-webserver = "^1.8.11"
dask = {extras = ["complete", "dataframe", "distributed"], version = "^2024.10.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.dagster]
module_name = "src"